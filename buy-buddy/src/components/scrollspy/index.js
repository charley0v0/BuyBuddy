import { withInstall } from '@/utils/withInstall';
import scrollspy from '@/components/scrollspy/scrollspy.vue';

const Scrollspy = withInstall(scrollspy);

// export components
export default { Scrollspy };

export { Scrollspy };
